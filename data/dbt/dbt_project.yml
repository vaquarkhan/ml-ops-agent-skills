name: mlops_platform
version: "1.0.0"
config-version: 2

profile: mlops_platform

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

seed-paths:
  - seeds

macro-paths:
  - macros

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  mlops_platform:
    +materialized: table
    +schema: staging

vars:
  min_row_count: 100
  max_null_rate: 0.01
